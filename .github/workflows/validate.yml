name: Validate Guidelines
on:
  pull_request:
    paths:
      - '.github/copilot/COPILOT-UPSTREAM.md'
      - '.vscode/**/*.json'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate Markdown
        run: |
          # Check common issues in upstream file
          ! grep -r "�" .github/copilot/COPILOT-UPSTREAM.md
          ! grep -r "Remember: The best guidelines" .github/copilot/COPILOT-UPSTREAM.md
          
          # Check file header for upstream file only
          if ! grep -q "🤖 MANAGED BY BC GOVERNMENT GUIDELINES" ".github/copilot/COPILOT-UPSTREAM.md"; then
            echo "Error: COPILOT-UPSTREAM.md missing managed file header"
            exit 1
          fi
